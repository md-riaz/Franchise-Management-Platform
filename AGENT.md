# Agent Guidelines

- Use date-based release identifiers in the `YYYY-MM-DD` format; do not append semantic version numbers.
- Every completed change must update `CHANGELOG.md` with the release date and a short description of what was delivered.
- Confirm the changelog update is in place before marking any task complete.
